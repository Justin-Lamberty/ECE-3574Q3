#include "polynomial.h"
int main(){
    std::vector<p1> = {3, 2, 2};
    std::vector<p2> = {1, 1, 1};
    std::vector<sum>;
    std::vector<diff>;
    sum = p1+p2;
    sum.print();

    diff = p1 - p2;
    diff.print();
}
