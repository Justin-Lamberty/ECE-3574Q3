#include "polynomial.h"
#include <vector>
int Polynomial::getTerms(p_terms){
    return coefficient;
    return exponent;
    
}
std::vector Polynomial::addition(p_terms){
    return <coefficient + coefficient, coefficient + coefficient, coefficient + coefficient>;
}
std::vector Polynomial::subtraction(p_terms){
    return <coefficient + coefficient, coefficient + coefficient, coefficient + coefficient>;
}

void print(){
    std::cout << "(" << coefficient << ")" << "  " << "(" << coefficient << ")" << "(" << coefficient << ")";
}